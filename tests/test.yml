---

- hosts: all
  name: Test ansible-python-packages role

  vars:
    python_packages:
      - six
      - name: pyelftools
      - name: requests-toolbelt

  roles:
    - role: ../../ansible-python-packages
